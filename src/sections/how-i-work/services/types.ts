export interface ServiceCardProps {
    icon: React.ReactNode;
    iconBg: string;
    title: string;
    description: string;
  }
  
  export interface Service {
    icon: React.ReactNode;
    iconBg: string;
    title: string;
    description: string;
  }